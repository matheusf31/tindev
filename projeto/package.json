{
  "name": "projeto",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "cd backend && yarn start",
    "build": "cd backend && npm install && cd .. && cd frontend && npm install && yarn start && cd .."
  }
}
